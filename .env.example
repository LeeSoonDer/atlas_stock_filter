# Copy to .env and fill locally. Never commit real secrets.

# Yahoo Finance (yahoo-finance2 npm package) requires no API key for the
# endpoints used by TASK_CARD_01 (quote, chart/OHLCV, quoteSummary).

# FMP (Financial Modeling Prep) - reserved for post-screen top-candidate
# enrichment per constitution/ATLAS_AMENDMENT_NO2_v1_1.md 实施架构.一.
# Not used by TASK_CARD_01.
FMP_API_KEY=

# Finnhub - reserved for news headlines / earnings calendar.
# Not used by TASK_CARD_01.
FINNHUB_API_KEY=

# FRED (Federal Reserve Economic Data) - reserved for macro engine inputs.
# Not used by TASK_CARD_01.
FRED_API_KEY=

# SEC EDGAR requires every requester to self-identify via User-Agent (a
# generic UA like "Mozilla/5.0" gets HTTP 403 - verified live). No signup,
# no key, just an honest contact string, e.g. "Your Name you@example.com".
# TASK_CARD_04's Form 4 fetcher falls back to a generic project-identifying
# string if this is left unset.
SEC_EDGAR_USER_AGENT=
